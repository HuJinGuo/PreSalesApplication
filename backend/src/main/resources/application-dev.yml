server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:13307/ai_smart?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&autoReconnect=true
    username: root
    password: qwe123999mn@123
    hikari:
      connection-test-query: SELECT 1
      minimum-idle: 2
      maximum-pool-size: 20
      idle-timeout: 300000
      max-lifetime: 900000
      keepalive-time: 300000
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  sql:
    init:
      mode: always
      schema-locations: classpath:init.sql
  flyway:
    enabled: false
    baseline-on-migrate: true
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 120MB

app:
  knowledge:
    storage-dir: /tmp/bid-knowledge-files
    embedding-concurrency: 6
    reindex-concurrency: 2
    # 词典启用：图谱实体关系基于人工领域词典抽取（企业可控）
    domain-lexicon-enabled: true
  vector:
    provider: milvus
    milvus:
      host: 120.27.212.35
      port: 19530
      collection: knowledge_chunk_vector
      embedding-dim: 1024
      nprobe: 16
      shards-num: 2
  upload:
    storage-dir: /tmp/bid-doc-uploads
  ai:
    provider: MOCK
    chat-provider: openai
    embedding-provider: openai
    chat-model: nvidia/nemotron-3-nano-30b-a3b
    # chat-model: z-ai/glm5
    embedding-model: baai/bge-m3
    keyword-model: moonshotai/kimi-k2.5
    openai:
      base-url: https://integrate.api.nvidia.com/v1
      api-key: 
      api-keys:  
        - 
        -
    alibaba:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: 
    baidu:
      auth-url: https://aip.baidubce.com/oauth/2.0/token
      chat-url: https://qianfan.baidubce.com/v2/chat/completions
      embedding-url: https://qianfan.baidubce.com/v2/embeddings
      api-key:
      secret-key:
    self-hosted:
      base-url: http://localhost:8000/v1
      api-key:
  security:
    jwt:
      secret: "hasd1zas12adas"
      expire-minutes: 720
  export:
    base-dir: /tmp/bid-doc-downloads
  exam:
    repeat-strategy: ALL
  agent:
    runtime:
      max-iterations-standard: 14
      max-iterations-fast-draft: 10
      tool-timeout-ms: 90000
      tool-max-retries: 1
      tool-retry-backoff-ms: 800
      tool-concurrency: 6

logging:
  level:
    root: INFO
    com.bidcollab: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
